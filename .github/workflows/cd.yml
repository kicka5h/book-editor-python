name: CD

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write   # required to create GitHub Releases and upload assets
  id-token: write   # required for PyPI OIDC trusted publishing
  packages: write   # required to push images to GitHub Container Registry

jobs:
  # ─────────────────────────────────────────────
  # Resolve the release version from pyproject.toml
  # ─────────────────────────────────────────────
  version:
    name: Resolve version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
      tag: ${{ steps.get_version.outputs.tag }}
      should_release: ${{ steps.gate.outputs.should_release }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract version from pyproject.toml
        id: get_version
        run: |
          VERSION=$(python - <<'EOF'
          import tomllib, pathlib
          data = tomllib.loads(pathlib.Path("pyproject.toml").read_text())
          print(data["project"]["version"])
          EOF
          )
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION"    >> "$GITHUB_OUTPUT"

      - name: Check milestone is complete
        id: check_milestone
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          # Derive milestone prefix from version: 3.4.1 → v3.4
          MILESTONE_VER="v$(echo "$VERSION" | cut -d. -f1,2)"

          MILESTONE=$(gh api repos/$REPO/milestones --paginate \
            --jq ".[] | select(.title | startswith(\"$MILESTONE_VER \"))" 2>/dev/null)

          if [[ -z "$MILESTONE" ]]; then
            echo "No milestone found matching '$MILESTONE_VER' — skipping milestone gate."
            echo "complete=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          OPEN=$(echo "$MILESTONE" | jq -r '.open_issues')
          MILESTONE_TITLE=$(echo "$MILESTONE" | jq -r '.title')
          MILESTONE_NUM=$(echo "$MILESTONE" | jq -r '.number')

          echo "Milestone : $MILESTONE_TITLE"
          echo "Open issues: $OPEN"

          if [[ "$OPEN" -gt 0 ]]; then
            echo ""
            echo "❌ '$MILESTONE_TITLE' still has $OPEN open issue(s) — release blocked:"
            gh api "repos/$REPO/issues?milestone=$MILESTONE_NUM&state=open" \
              --jq '.[] | "  #\(.number) \(.title)"'
            echo "complete=false" >> "$GITHUB_OUTPUT"
          else
            echo "✅ Milestone '$MILESTONE_TITLE' is complete."
            echo "complete=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Check if tag already has a release
        id: check_tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v${{ steps.get_version.outputs.version }}"
          if gh release view "$TAG" > /dev/null 2>&1; then
            echo "tag_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "tag_exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Gate release
        id: gate
        run: |
          MILESTONE_COMPLETE="${{ steps.check_milestone.outputs.complete }}"
          TAG_EXISTS="${{ steps.check_tag.outputs.tag_exists }}"
          TAG="v${{ steps.get_version.outputs.version }}"

          if [[ "$MILESTONE_COMPLETE" != "true" ]]; then
            echo "🚫 Release blocked: milestone not complete. Close all open issues first."
            echo "should_release=false" >> "$GITHUB_OUTPUT"
          elif [[ "$TAG_EXISTS" == "true" ]]; then
            echo "Release $TAG already exists — skipping. Bump the version in pyproject.toml for a new release."
            echo "should_release=false" >> "$GITHUB_OUTPUT"
          else
            echo "✅ All checks passed — proceeding with release."
            echo "should_release=true" >> "$GITHUB_OUTPUT"
          fi

  # ─────────────────────────────────────────────
  # Build the packaged app on each platform
  # ─────────────────────────────────────────────
  build:
    name: Build (${{ matrix.platform_name }})
    needs: version
    if: needs.version.outputs.should_release == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform_name: Windows
            artifact_name: Beckit-Windows
          - os: macos-latest
            platform_name: macOS
            artifact_name: Beckit-macOS

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -e .
          pip install flet pyinstaller

      - name: Inject OAuth client ID
        shell: python
        env:
          OAUTH_CLIENT_ID: ${{ secrets.OAUTH_CLIENT_ID }}
        run: |
          import os, pathlib, re
          path = pathlib.Path("src/book_editor/services/github_app.py")
          src = path.read_text(encoding="utf-8")
          client_id = os.environ["OAUTH_CLIENT_ID"]
          src = re.sub(r'^_OAUTH_CLIENT_ID\s*=\s*""', f'_OAUTH_CLIENT_ID = "{client_id}"', src, flags=re.MULTILINE)
          path.write_text(src, encoding="utf-8")

      - name: Build app with flet pack (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: >
          flet pack src/book_editor/__main__.py
          --name "Beckit"
          --product-name "Beckit"
          --file-description "A desktop app for writing books with GitHub version control"
          -D

      - name: Download and embed Pandoc (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Invoke-WebRequest `
            -Uri "https://github.com/jgm/pandoc/releases/download/3.6.2/pandoc-3.6.2-windows-x86_64.zip" `
            -OutFile "$env:TEMP\pandoc.zip"
          Expand-Archive "$env:TEMP\pandoc.zip" -DestinationPath "$env:TEMP\pandoc-bin" -Force
          New-Item -ItemType Directory -Force -Path "dist\Beckit\bin" | Out-Null
          Copy-Item "$env:TEMP\pandoc-bin\pandoc-3.6.2\pandoc.exe" "dist\Beckit\bin\pandoc.exe"

      - name: Download, pre-seed, and embed TinyTeX (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          TINYTEX_VERSION: v2026.07  # pinned; 'daily' asset names change without notice
        run: |
          Invoke-WebRequest `
            -Uri "https://github.com/rstudio/tinytex-releases/releases/download/$env:TINYTEX_VERSION/TinyTeX-1-windows-$env:TINYTEX_VERSION.exe" `
            -OutFile "$env:TEMP\tinytex.exe"
          # Windows binaries are 7-Zip self-extracting archives
          7z x "$env:TEMP\tinytex.exe" -o"$env:TEMP\tinytex-extract" -y | Out-Null
          # Locate tlmgr.bat rather than assuming the top-level folder name
          $tlmgr = Get-ChildItem "$env:TEMP\tinytex-extract" -Recurse -Filter tlmgr.bat | Select-Object -First 1
          $tinytexDir = $tlmgr.Directory.Parent.Parent.FullName
          # Snapshot tlmgr refuses to install if the repo has a newer tlmgr
          & $tlmgr.FullName update --self
          & $tlmgr.FullName install geometry fancyhdr hyperref xcolor caption float listings lm booktabs
          Copy-Item -Recurse -Force "$tinytexDir" "dist\Beckit\tinytex"

      - name: Build app with flet pack (macOS)
        if: runner.os == 'macOS'
        run: >
          flet pack src/book_editor/__main__.py
          --name "Beckit"
          --product-name "Beckit"
          --file-description "A desktop app for writing books with GitHub version control"

      - name: Download and embed Pandoc (macOS)
        if: runner.os == 'macOS'
        run: |
          ARCH=$(uname -m)
          curl -fsSL "https://github.com/jgm/pandoc/releases/download/3.6.2/pandoc-3.6.2-${ARCH}-macOS.zip" \
               -o /tmp/pandoc.zip
          unzip -q /tmp/pandoc.zip -d /tmp/pandoc-bin
          mkdir -p dist/Beckit.app/Contents/Resources/bin
          cp /tmp/pandoc-bin/pandoc-3.6.2-${ARCH}/bin/pandoc \
             dist/Beckit.app/Contents/Resources/bin/pandoc
          chmod +x dist/Beckit.app/Contents/Resources/bin/pandoc

      - name: Download, pre-seed, and embed TinyTeX (macOS)
        if: runner.os == 'macOS'
        env:
          TINYTEX_VERSION: v2026.07  # pinned; 'daily' asset names change without notice
        run: |
          curl -fsSL "https://github.com/rstudio/tinytex-releases/releases/download/${TINYTEX_VERSION}/TinyTeX-1-darwin-${TINYTEX_VERSION}.tar.xz" \
               -o /tmp/tinytex.tar.xz
          mkdir -p /tmp/tinytex-extract
          tar -xJf /tmp/tinytex.tar.xz -C /tmp/tinytex-extract
          # Locate tlmgr rather than assuming the top-level folder name
          # (-type l too: tlmgr is a symlink in the TeX Live bin dir)
          TLMGR=$(find /tmp/tinytex-extract \( -type f -o -type l \) -path "*/bin/*/tlmgr" | head -1)
          if [[ -z "$TLMGR" ]]; then
            echo "::error::tlmgr not found in extracted TinyTeX; archive layout:"
            find /tmp/tinytex-extract -maxdepth 4 | head -40
            exit 1
          fi
          TINYTEX_DIR=$(dirname "$(dirname "$(dirname "$TLMGR")")")
          # Snapshot tlmgr refuses to install if the repo has a newer tlmgr
          "$TLMGR" update --self
          "$TLMGR" install geometry fancyhdr hyperref xcolor caption float listings lm booktabs
          mkdir -p dist/Beckit.app/Contents/Resources/tinytex
          cp -R "$TINYTEX_DIR/." dist/Beckit.app/Contents/Resources/tinytex/

      - name: Patch Info.plist bundle name (macOS)
        if: runner.os == 'macOS'
        run: |
          PLIST="dist/Beckit.app/Contents/Info.plist"
          /usr/libexec/PlistBuddy -c "Set :CFBundleName Beckit" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName Beckit" "$PLIST"
          echo "CFBundleName is now: $(/usr/libexec/PlistBuddy -c 'Print :CFBundleName' "$PLIST")"

      # ── Windows: build NSIS installer ──
      - name: Install NSIS (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: choco install nsis --no-progress -y

      - name: Build Windows NSIS installer
        if: runner.os == 'Windows'
        shell: pwsh
        working-directory: installer\windows
        run: |
          & "C:\Program Files (x86)\NSIS\makensis.exe" `
            /DVERSION=${{ needs.version.outputs.version }} `
            /V4 `
            installer.nsi

      # ── macOS: build PKG installer ──
      - name: Build macOS PKG installer
        if: runner.os == 'macOS'
        run: |
          chmod +x installer/macos/build-pkg.sh installer/macos/scripts/postinstall
          bash installer/macos/build-pkg.sh ${{ needs.version.outputs.version }}

      - name: Upload Windows installer artifact
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: installer\windows\Beckit-Windows-${{ needs.version.outputs.version }}-installer.exe
          retention-days: 7

      - name: Upload macOS installer artifact
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/Beckit-macOS-${{ needs.version.outputs.version }}.pkg
          retention-days: 7

  # ─────────────────────────────────────────────
  # Create the GitHub Release and attach assets
  # ─────────────────────────────────────────────
  release:
    name: Create GitHub Release
    needs: [version, build]
    if: needs.version.outputs.should_release == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Collect release assets
        run: |
          mkdir -p release-assets
          find artifacts/ -type f \( -name "*.exe" -o -name "*.pkg" \) \
            -exec cp {} release-assets/ \;
          echo "Assets to publish:"
          ls -lh release-assets/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.version.outputs.tag }}
          name: "Beckit ${{ needs.version.outputs.tag }}"
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## Beckit ${{ needs.version.outputs.tag }}

            ### Installation

            **macOS**
            1. Download `Beckit-macOS-${{ needs.version.outputs.version }}.pkg`
            2. Double-click the `.pkg` file and follow the installer wizard
            3. On first launch, right-click → Open if macOS warns about an unverified developer

            > **Note:** The installer is unsigned. If Gatekeeper blocks it, right-click the `.pkg` → Open,
            > or temporarily allow it via System Settings → Privacy & Security.

            **Windows**
            1. Download `Beckit-Windows-${{ needs.version.outputs.version }}-installer.exe`
            2. Run the installer (Administrator rights required)
            3. Launch **Beckit** from the Start Menu or Desktop shortcut

            ### Requirements
            - A GitHub account (sign-in uses GitHub Device Flow — no Personal Access Token needed)
            - PDF export is built in — Pandoc and pdflatex are bundled, no separate install needed
          files: release-assets/*

  # ─────────────────────────────────────────────
  # Publish to PyPI via OIDC Trusted Publisher
  # (no API token needed — configure the trusted
  #  publisher on PyPI before this will succeed)
  # ─────────────────────────────────────────────
  publish-pypi:
    name: Publish to PyPI
    needs: [version, release]
    if: needs.version.outputs.should_release == 'true'
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/beckit

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Build source and wheel distributions
        run: |
          pip install --upgrade build
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  # ─────────────────────────────────────────────
  # Publish builds to itch.io via butler
  # Requires BUTLER_CREDENTIALS secret (itch.io API key)
  # ─────────────────────────────────────────────
  itch-publish:
    name: Publish to itch.io
    needs: [version, release]
    if: needs.version.outputs.should_release == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Install butler
        run: |
          curl -fsSL \
            "https://broth.itch.zone/butler/linux-amd64/LATEST/archive/default" \
            -o butler.zip
          unzip -q butler.zip
          chmod +x butler
          ./butler --version

      - name: Push Windows build to itch.io
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_CREDENTIALS }}
        run: |
          ./butler push \
            "artifacts/Beckit-Windows/Beckit-Windows-${{ needs.version.outputs.version }}-installer.exe" \
            kicka5h/beckit-book-editor:windows \
            --userversion "${{ needs.version.outputs.version }}"

      - name: Push macOS build to itch.io
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_CREDENTIALS }}
        run: |
          ./butler push \
            "artifacts/Beckit-macOS/Beckit-macOS-${{ needs.version.outputs.version }}.pkg" \
            kicka5h/beckit-book-editor:mac \
            --userversion "${{ needs.version.outputs.version }}"

  # ─────────────────────────────────────────────
  # Build and publish Docker image to GHCR
  # Image is tagged with the release version and
  # :latest so developers can always pull the
  # most recent stable container.
  # ─────────────────────────────────────────────
  docker-publish:
    name: Publish Docker image to GHCR
    needs: [version, release]
    if: needs.version.outputs.should_release == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/beckit:${{ needs.version.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/beckit:latest
